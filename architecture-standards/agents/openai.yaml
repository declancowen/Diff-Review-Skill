interface:
  display_name: "Architecture Standards"
  short_description: "Code-oriented architecture and boundary guidance"
  default_prompt: "Use $architecture-standards to design or build code with clear ownership, enforceable boundaries, proportional architecture, and practical tradeoffs."

policy:
  allow_implicit_invocation: true
