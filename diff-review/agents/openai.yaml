interface:
  display_name: "Diff Review"
  short_description: "Review diffs, external findings, and escaped bug patterns"
  default_prompt: "Use $diff-review to review my current git diff for bugs, regressions, external findings, and open issues before I push."
