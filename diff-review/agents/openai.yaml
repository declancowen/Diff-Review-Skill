interface:
  display_name: "Diff Review"
  short_description: "Review diffs for bugs and architecture drift"
  default_prompt: "Use $diff-review to review my current git diff for bugs, regressions, analyzer-policy issues, architecture drift, and open findings before I push."
